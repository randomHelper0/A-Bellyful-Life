// Inherit the parent event
event_inherited();

if (checkAmberEvent()){
	/*time_forward_minutes(60);
	ControlEnv.hours = 7;
	ControlEnv.minutes = 50;
	global.scene_script = amber_jog;
	scene_start();*/
}